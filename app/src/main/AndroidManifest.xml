<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".guardian_of_the_galaxy"></activity>
        <activity android:name=".BinLocation" />
        <activity android:name=".studentRegisterPage" />
        <activity android:name=".studentLoginPage" />
        <activity android:name=".stakerholder_login_page" />
        <activity android:name=".BarcodePage" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BinStatsPage" />
        <activity android:name=".StakeholderPage" />
        <activity android:name=".ViewCreditsPage" />
        <activity android:name=".ZoomlionOptionPage" />
    </application>

</manifest>